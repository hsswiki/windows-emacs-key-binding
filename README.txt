# Use Emacs and Mac-like Key Bindings on Windows PC

Usage: On Windows, download [`EmacsKeyBinding.exe`](EmacsKeyBinding.exe) and double-click. An "H" icon will appear in the system tray when the software is running.

See the header of [`EmacsKeyBinding.ahk`](EmacsKeyBinding.ahk) for detailed documentation.
